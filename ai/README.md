# AI

This folder contains the AI-specific engineering and evaluation layer.

- `eval/` stores test cases, rubrics, and evaluation notes.
- `rag/` stores retrieval architecture and knowledge-source documentation.

The AI layer is intentionally separated from product strategy so model quality and safety can be evaluated independently.